import 'package:n8ndistribution/models/routing_model.dart';

class AppRoute {
  // Existing routes (example, if any)
  // static RoutingModel home = const RoutingModel(name: "home", path: "/");

  // New route for LoginScreen
  static RoutingModel login = const RoutingModel(name: "login", path: "/login");
}
